version: 2
jobs:
  build:
    branches:
      only:
        - test-ci
    docker:
      - image: node:latest
    steps:
      - checkout
      - run: npm i
      - run: npm run test
      - run: npm run build